# Core (paper trading + dashboard). Everything the app needs today.
# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Config / validation
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# HTTP client for Polymarket public data-api
httpx==0.28.1

# ── Live trading (Phase 4) deps live in requirements-live.txt ──
# py-clob-client is only needed once real CLOB execution is wired up. Keeping it
# out of the default install keeps the Docker image small and the build reliable.
